project_id = "ivdymghkcwgwoedrruhx"

# Higienizador IA (Supabase opcional): chave OpenAI em Secrets, não no frontend.
# HostGator usa api/lib/ia.php (Gemini/OpenAI). verify_jwt = false: sb_publishable_ não é JWT.
[functions.higienizador-ia]
verify_jwt = false
